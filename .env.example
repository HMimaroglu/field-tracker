# Field Tracker Configuration
# Copy this file to .env and customize for your deployment

# Database Configuration
DB_PASSWORD=secure_password_123
DB_PORT=5432

# Server Configuration
JWT_SECRET=your_jwt_secret_change_this_in_production
ADMIN_PASSWORD=admin123
SERVER_PORT=3000
ADMIN_PORT=3001

# External URLs (for production deployment)
API_URL=http://localhost:3000
CORS_ORIGINS=http://localhost:3001,https://yourdomain.com

# Storage Configuration
# For local storage (default):
STORAGE_TYPE=local

# For S3 storage (uncomment and configure):
# STORAGE_TYPE=s3
# S3_BUCKET=your-bucket-name
# S3_REGION=us-east-1
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_ENDPOINT=https://s3.amazonaws.com

# License System
LICENSE_PUBLIC_KEY=

# Nginx Configuration (optional)
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# Production Security Notes:
# 1. Change all default passwords before deployment
# 2. Use strong, unique JWT secrets (64+ characters)
# 3. Configure proper CORS origins for your domain
# 4. Set up SSL certificates for HTTPS
# 5. Configure S3 or secure local storage for file uploads
# 6. Ensure LICENSE_PUBLIC_KEY is properly set for license validation